<h2>Category list</h2>
<hr>
<a href="/admin/categories/addcategory"><button class="btn btn-success">New Category</button></a>

{{#each categories}}
    <div class="card mt-4">
        <div class="card-body">
            <h4>{{name}}</h4>
            <small>{{slug}}</small>
        </div>
    </div>

{{else}}

{{/each}}